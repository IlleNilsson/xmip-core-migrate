# Its own workspace root, so this repository builds on its own.
[workspace]

[package]
name = "xmip-core-migrate"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
repository = "https://github.com/IlleNilsson/xmip-core-migrate"
description = "Starter template for a Rust-based Xmip component repository."

[lib]
path = "src/lib.rs"

[lints.rust]
unsafe_code = "forbid"
