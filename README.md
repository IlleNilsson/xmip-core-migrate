# xmip-core-migrate

Reads a source integration platform's configuration and produces Xmip artifacts, together with a report of anything that has no Xmip equivalent.

It translates configuration and reports gaps. It does not emulate the source runtime or silently invent replacements.

Status: planned; no public migration API is implemented yet.
